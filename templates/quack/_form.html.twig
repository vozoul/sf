{{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.content) }}
    {{ form_row(form.imageFile) }}
    {{ form_row(form.author, {attr: {value: app.user.duckname }}) }}
    {{ form_rest(form) }}
    <button class="btn btn-primary float-right">{{ button | default('Save') }}</button>
{{ form_end(form) }}